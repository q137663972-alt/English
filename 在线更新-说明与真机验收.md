# 在线更新 · 使用说明与真机验收

## 一、现在是什么状态

装了 **1.2**（`versionCode 3`）之后：

| 想改什么 | 还要不要你手动装包 |
|---|---|
| 题目、生字、古诗、单词 | ❌ 不用 |
| 界面、新玩法、修 bug | ❌ 不用 |
| 原生层（语音识别、权限、图标） | ✅ 要，但 App 里会弹一条提示，点一下就装 |

**1.2 是最后一次需要你手动传文件安装的包。**（安卓不允许第三方 App 静默安装，所以「完全后台无感」做不到——除了上架应用商店。）

---

## 二、以后的更新流程（你或我）

1. 我改 `/workspace/{,chinese/,math/}` 里的 H5 代码（`index.html`、`css/`、`js/`，**除了 `js/boot.js`**）
2. 推到 GitHub → CI 自动生成热更包并发布到 Pages
3. 孩子**打开 App 两次**：第一次后台悄悄下载，**第二次打开就是新的**
   - 也可以只打开一次、玩一会儿，第二天再打开也是新的
   - **全程不用装包、不用点任何东西、不打断做题**

> ⚠️ **`js/boot.js` 是冻结文件**，改它必须出新 APK（它会层层校验，没法热更自己）。

---

## 三、真机验收（装完 1.2 做这 5 步）

1. **装 1.2**：签名没变（`ffbc7d65…`），可以直接覆盖安装，**星星进度不会丢**。
2. **打开，随便玩一局** —— 必须和之前一模一样，没有白屏、没有变慢。
3. **看热更日志（可选）**：设置面板里连点 **5 次「⚙️ 设置」** 这个标题 → 页面会刷新并显示热更日志，里面能看到：
   - `base=…` 热更地址对不对
   - `fetch=` / `localStorage=` 是否可用
   - `build=…` 有没有值（有值 = 已经存过热更版本）
   - `use cache` / `use builtin` 本次用的是哪一层
   - `BOOT OK` = 启动成功
   看完按返回键退出日志页即可。
4. **验证真的会热更**：告诉我一声，我把首页改一个字（比如「开始学习」改成「开始闯关」）推上去，你**打开 App 两次**，应该看到字变了。
5. **验证一键升级**：下次我出新 APK 时，首页顶部会出现一条「🎉 有新版本…[更新] [×]」，点「更新」→ 系统会弹一次安装确认 → 装完自动是新版本。

---

## 四、出问题怎么办

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 打开白屏 | 热更包有问题 | **不会持续白屏**：哨兵 1.5 秒内发现没渲染出来 → 自动清空热更缓存 → 重启用内置版本。连错两次会永久关闭热更，回到和现在一样 |
| 一直没更新 | Pages 连不上（国内访问 github.io 时快时慢） | 不影响使用，下次打开会再试。想确认就去浏览器打开 `https://q137663972-alt.github.io/chinese/hot/manifest.js` 看能不能看到内容 |
| 想强制用内置版 | — | 卸载重装，或用 adb 清 App 数据 |
| 语文的笔顺/图库没更新 | 这两个文件大（1.2MB + 260KB），localStorage 可能装不下 | 设计成「尽力而为」：装不下就继续用内置的，其他文件照常更新。真要强制更新笔顺数据时，出一个新 APK |

---

## 五、还需要你做一件事

**英语仓库（仓库名 `English`）的 Pages 没开过**，热更包发不出去：

- 打开 `https://github.com/q137663972-alt/English/settings/pages`
- Source 选 **Deploy from a branch**，Branch 选 **`gh-pages`** / `/(root)`，保存

语文和数学已经跑通了，不用管。

---

## 六、代价与风险（我得跟你说清楚）

1. **APK 变成公开可下载了**：应用内升级需要一个能直接下载的 https 地址，所以 CI 会把签名好的 APK 一起发布到 Pages（`https://…/chinese/apk/ChinesePlayground-Phone.apk`）。任何人拿到链接都能装。
2. **全量热更 = 远程代码执行**：万一 GitHub 账号被盗，攻击者改热更包就能在所有已装设备上跑他的代码。**强烈建议**：GitHub 开两步验证（2FA）、给 `gh-pages` 分支加保护规则。
3. **将来想上 Google Play 会违规**（Play 明令禁止从 APK 外加载可执行代码）。国内商店没这个问题。真要上 Play，得留一个远程关闭热更的开关（机制已预留：`hot.disabled`）。
4. **keystore 还是那个**：`/workspace/keystore/playground-release.jks`，丢了以后所有升级都要卸载重装 —— **务必备份整个 `keystore/` 目录到网盘**。

---

## 七、技术备注（给自己/给我看）

- 三层架构：内置 assets → 本地缓存（localStorage，内容寻址）→ 远程 `hot/`
- **启动路径零网络**：有缓存用缓存，没缓存用内置，都是纯本地同步执行；网络只发生在启动成功 3 秒后的后台
- 文件分两档：核心文件（全齐才切版本）+ 大文件（语文 `strokes.js`/`pics.js`，尽力而为）
- 原子提交 = 只写 `hot.build` 一个 key；文件早已按内容哈希存好并校验过
- 生成器 `tools/gen-hot.mjs`：从 `js/boot.js` 的 `BUILTIN` 读顺序，从 `index.html` 的 `<!-- shell:begin/end -->` 抽 shell，**不需要维护第二份文件**
- `js/update.js`：启动 4 秒后查一次 `hot/update.js`，有新版本才显示顶部提示条；`AndroidUpdate.install()` 里**硬编码了 APK 域名白名单**，防止远程 JS 装马
