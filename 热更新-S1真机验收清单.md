# S1 幂等化 · 真机验收清单

S1 只做「幂等挂载」改造，**零网络、零行为变化**。装上后如果和之前一模一样，就算通过。

## ⚠️ 先看这条：签名换了，必须先卸载旧版

`2026-09-18` 起，6 个 APK 改用**正式发布签名**（之前 CI 出的是 debug 签名，两者不兼容）。

- 手机上**已装旧版** → 直接装会报「应用未安装」，**必须先卸载旧版**；
- 卸载会清掉 App 数据，**星星进度会丢** —— 装之前让孩子把首页星星数截个图留念；
- 只有这一次痛。**从今往后本地包和 CI 包同签名，可以直接覆盖安装，不再丢进度。**

## 安装

`/workspace/apk/` 下 6 个包，挑你手上设备对应的装（**覆盖安装，别卸载**，否则星星进度会丢）：

| APK | 设备 |
|---|---|
| `ChinesePlayground-TV.apk` | 语文 · 电视/盒子 |
| `ChinesePlayground-Phone.apk` | 语文 · 手机 |
| `MathPlayground-TV.apk` | 数学 · 电视/盒子 |
| `MathPlayground-Phone.apk` | 数学 · 手机 |
| `EnglishPlayground-TV.apk` | 英语 · 电视/盒子 |
| `EnglishPlayground-Phone.apk` | 英语 · 手机 |

## 验收 4 项（每个装上的包都看一遍）

1. **打开不是白屏** —— 首页能看到「共 6 个年级 · 48 个单元」（英语是 72 个单元）。
2. **星星数一个没变** —— 首页「🌟 我的星星」数字应与改造前完全一致。这是最要紧的一条。
3. **年级卡片齐全** —— 点「开始学习」，6 张年级卡都在，进任意年级单元列表完整。
4. **随便玩一局** —— 听音选字、看图识字、笔顺演示各点一次，正常出题、正常给星，退出后星星 +1。

## 出问题怎么回滚

把设备上这个 App 卸了，装回上一版 APK 即可；或者告诉我，我删掉 `index.html` 里 `js/cp.js` 那一行重打——
**数据文件已写成 `window.CP ? CP.setGrade(...) : 原push兜底`**，加载器不在也能正常跑（这条我已在浏览器里验证过）。

## S2 生成器现状（本地已跑通，未上线）

`tools/gen-content.mjs` 已生成内容包，等 S4/S5 接线后才会真正被 App 拉：

| 项目 | 分片 | 总体积 |
|---|---|---|
| 语文 | 20 片（6 年级 × 生字/笔顺/插图 + 古诗 + 词语） | 1491 KB |
| 数学 | 1 片 | 45.5 KB |
| 英语 | 1 片 | 53.3 KB |

已验证：连跑两次分片 hash 完全一致；改「霜」字的笔顺后**只有 `g3-s` 一片的 hash 变**，其余 19 片命中缓存不用重下。

## 下一步（等你验收完再动）

- S3 真机探针：确认 WebView 里 IndexedDB 能用，`file://` 页能执行 https 的 js。
- S4 加载器：补齐 `apply/ok/start` + 15 秒超时 + 三级缓存降级。
- S5 CI 发布：语文/数学的 pages job 加两行；**英语仓库需要你先在 GitHub 网页端 Settings → Pages 手动开一次（选 `gh-pages` 分支）**。
