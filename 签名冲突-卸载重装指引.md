# 安装失败「已安装了签名冲突的应用」——卸载重装指引

## 一、为什么必须卸载

安卓有一条硬规则：**同一个包名（applicationId），签名不一样就不允许覆盖安装。**

| | 你现在手机上装的 | 新出的包 |
|---|---|---|
| 来源 | GitHub Actions 自动构建 | 本地 `build-apk.sh` 构建 |
| 签名 | **debug 签名**（每次 CI 在全新虚拟机上临时生成，每次都不同） | **正式发布签名**（统一钥匙，SHA-256 `ffbc7d65…`） |
| 结果 | — | 签名不一致 → 覆盖安装被拒 |

所以这不是包坏了，是安卓在保护你：它怕有人拿同名的假包替换掉你的 App。

**代价**：卸载会清掉 App 数据，孩子的星星进度会丢。这次躲不过（旧包是 debug 签名，没法沿用它）。**但这次之后永远不会再发生**——新包现在是固定签名，以后每次升级都是直接覆盖安装，进度不丢；而且新版里加了「进度备份/恢复」，见第三节。

---

## 二、现在就做（5 分钟）

### 步骤 0：先截图留念（别跳过）

打开旧版 → 首页右上角「🌟 我的星星 <数字>」→ **截图**。
三个 App 各截一张。这是孩子这段时间攒下的东西，卸载后旧版就读不出来了。

### 步骤 1：卸载旧版

桌面长按 App 图标 → 「卸载」/「应用信息」→「卸载」。
或者：设置 → 应用管理 → 找到 App 名 → 卸载。

**TV 版和手机版是两个不同的包，装了几个就要卸几个：**

| App | TV 版包名 | 手机版包名 |
|---|---|---|
| 语文乐园 | `com.example.chineseplayground` | `com.example.chineseplayground.phone` |
| 数学乐园 | `com.example.mathplayground` | `com.example.mathplayground.phone` |
| 英语乐园 | `com.example.englishplayground` | `com.example.englishplayground.phone` |

> 判断方法：卸载的时候看图标——TV 版图标是横版大图（给电视/平板用），手机版是竖版图标。两个都装了就都卸掉，只装了一个就卸一个。

### 步骤 2：装新版

把 `/workspace/apk/` 里这几个文件传到手机（微信文件传输助手 / 数据线 / QQ 都行）：

```
ChinesePlayground.apk（语文 · 三端通用）
MathPlayground.apk（数学 · 三端通用）
EnglishPlayground.apk（英语 · 三端通用）
```

点开安装。如果手机提示「未知来源」，按提示允许一次即可（这是正常提示，因为不是从应用商店装的）。

### 步骤 3：确认装上了

能正常打开、首页出现年级卡片，就成功了。
以后**再更新版本，直接装新包覆盖就行，不用再卸载，进度也不会丢**。

---

## 三、新版里的「进度备份 / 恢复」（这次之后永不丢）

三个 App 的设置面板（右上角 ⚙️）都多了一行 **「进度备份」**，两个按钮：

- **💾 导出**：把全部星星进度变成一串文本，自动复制到剪贴板，并显示在下方文本框里。
  → 粘到微信收藏 / 备忘录 / 发给自己的文件传输助手，就永久存住了。
- **📥 恢复**：把之前存的文本粘进下方文本框，点恢复，星星全部回来。

**建议习惯**：每周或每次孩子攒了不少星星时，导出一次存到微信收藏。换手机、重装、甚至换签名，都能一键粘回来。

> 导出文本长这样（每个 App 的 `app` 字段不同，别串着粘）：
> ```json
> {"v":1,"app":"cn","ts":1758123456789,"settings":{"tts":true,"rate":0.85},"progress":{"0-0-0":3,"0-0-1":2}}
> ```

---

## 四、可选：抢救旧进度（大概率不行，别折腾太久）

**只有同时满足两个条件才可能成功**：

1. 手机开了 **USB 调试**（开发者选项里）；
2. 电脑装了 **adb**（Android SDK platform-tools）。

旧版是 CI 的 debug 构建，自带 `debuggable`，理论上可以用 `run-as` 把 WebView 的 localStorage 文件拉出来：

```bash
adb exec-out run-as com.example.chineseplayground \
  cat /data/data/com.example.chineseplayground/app_webview/Local\ Storage/leveldb/000003.log \
  > cn_progress.bak
```

然后从 `cn_progress.bak` 里找到 `cn_progress` 开头的 JSON，抠出来粘进新版的「恢复」框里。

**不满足条件、或者试了两次没出来，就别折腾了**，按丢处理。孩子重新攒星星其实很快，而且这次之后有备份了。

---

## 五、打包相关（给自己看的备注）

- keystore：`/workspace/keystore/playground-release.jks`，alias `playground`
- 证书 SHA-256：`ffbc7d65ead51e3faa2c456482d4077dd7bfb39d592fa59467d5ec336d923076`
- 签名方式：`apksigner`，v1 + v2 开，v3/v4 关（minSdk 21）
- **keystore 丢了 = 以后所有升级都要卸载重装**，务必备份 `/workspace/keystore/` 整个目录到网盘。
- CI 要出正式包需要 4 个 Secrets，填写清单在 `/workspace/keystore/GitHub-Secrets填写清单.md`（用完删掉那份清单）。
