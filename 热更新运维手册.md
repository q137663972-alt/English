# 小学学习 App · 热更新运维手册

> 最后更新：2026-09-19
> 适用：英语乐园 / 语文乐园 / 数学乐园 三个 App（H5 + Android WebView 壳）
> 读者：执行更新的助手（照着做）+ 项目负责人（看懂、能自查）
>
> ⚠️ 本手册**不含**任何明文 token、keystore 密码或密钥内容，只记录「去哪找」。

---

## 0. 一句话

**改代码 → 推 GitHub → CI 自动发包 → 孩子打开 App 两次生效。全程不用装包。**

唯一例外：改 `js/boot.js`（冻结文件）或原生层，才需要出新 APK。

---

## 1. 三个科目速查表

| 科目 | 本地目录（独立 git 仓库） | 仓库 | 远程（镜像） | 热更地址 | 大文件 | 当前版本 |
|---|---|---|---|---|---|---|
| 英语 | `/workspace`（仓库根） | `q137663972-alt/English` | ghproxy.net | `…github.io/English/hot/` | 无 | 1.2（code 3） |
| 语文 | `/workspace/chinese` | `q137663972-alt/chinese` | ghfast.top | `…github.io/chinese/hot/pack/` | `strokes.js` `pics.js` | **2.0（code 3）· v3.0 通用壳** |
| 数学 | `/workspace/math` | `q137663972-alt/math` | ghfast.top | `…github.io/math/hot/` | 无 | 1.1（code 2） |

- **三科是三个独立 git 仓库**，各自有自己的 `remote.origin.url` 与提交历史；`/workspace` 根仓库通过 `.gitignore` 的 `/chinese/` `/math/` 把它们排除在外
- 三个仓库都是 **public**，Pages 从 `gh-pages` 分支 `/ (root)` 发布
- 旧仓库 `12` 已废弃（英语原仓库，保留但不再维护）
- 脚本位置注意：`sync-assets.sh` 与 `build-apk.sh` **只存在于英语项目根目录**，但它们会遍历三个科目；
  语文 / 数学目录里**没有**这两个脚本的副本

---

## 2. 热更是怎么工作的

### 三层加载

```
内置 assets  →  localStorage 内容寻址缓存  →  远程 hot/
   ①                    ②                        ③
```

> **语文 v2.0 起改走 §2.5 的文件级通道**（不再用 localStorage）。英语 / 数学仍是上面这条老通道。

**启动路径零网络**：启动时先看本地有没有缓存，有就用；没有就用内置版本。
网络请求只发生在**启动成功 3 秒之后的后台**，所以「Pages 挂了」只会退化成「这次没更新」，永远不会白屏、不会卡。

### 关键设计

- **内容寻址**：`hot.c.<hash>` 存文件正文。文件没改动就零下载，天然去重
- **原子提交**：`hot.i.<build>` 是清单，切换版本只需改写 `hot.build` 这一个 key
- **校验**：FNV-1a 双通道 32bit → 16 位 hex，同步计算（不依赖 `crypto.subtle`）
- **文件分两档**
  - 核心文件：必须全部就位才切版本（all-or-nothing）
  - 大文件：尽力而为。存不下就继续用内置版本，不影响其他文件更新
    （语文 `strokes.js` 1.2MB + `pics.js` 260KB 属于这一档）
- **四道保险**
  1. `#nohot` 逃生开关（URL 参数）
  2. 启动哨兵 1.5 秒内没渲染出来 → 自动清空缓存重载一次
  3. 连续 2 次失败 → 永久关闭热更，回到纯内置
  4. APK 版本号变了 → 自动重置上述状态

---

## 2.5 语文 v3.0：通用壳 + 文件级热更（2026-09-19 上线）

> 英语 / 数学暂未迁移，本节只描述语文。

### 为什么改

老通道把文件正文塞 localStorage，配额只有 5MB。实测语文内置 js+css+shell 已占 2.99MB，
567 张图再进去必爆配额，而且 `pics.js` 一爆就整体回退内置 —— 一张新图都看不到。

### 新架构

```
内置 assets  →  files/hot/（zip 解压落盘）  →  远程 hot/pack/*.zip
   ①                    ②                          ③
```

- **任意格式都能热更**：js / css / 图片 / 音频 / 字体，想多大就多大
- **玩法热更**：`window.registerGame({id,name,icon,desc,start})`；
  `tools/gen-pack.mjs` 扫 `js/game-*.js` 写入 `MANIFEST.json` 的 `games[]`，
  boot 在 `js/games.js` 之后逐个 `try/catch` 加载 —— 单个玩法挂了只掉一个入口，不白屏
- **读取靠虚拟域**：`shouldInterceptRequest` 把 `https://local.hot/<p>` → `files/hot/<p>`
- **双 slot 原子切换**：解压到 `hot.new/` → 校验 → rename 成 `hot/`；旧版留 `hot.old/`
- **多分包叠加**：先 `copyDir(hotDir(), staging)` 再解压覆盖，所以 assets.zip 与 code.zip
  可以依次装、不互相抹掉。**顺序必须是 assets 在前、code（含 MANIFEST）在后**

### 一个 APK 通吃三端

`com.example.chineseplayground` v3（旧 TV 包同名同签 → 可原地覆盖升级；
旧 Phone 包是 `…chineseplayground.phone`，会并存成另一个 App）。

- 设备判定三重兜底：`UiModeManager==TELEVISION` ‖ `FEATURE_LEANBACK` ‖ `!FEATURE_TOUCHSCREEN`
- `body` 上打 `tv` / `tablet` / `phone`，CSS 断点**只用 `min-width`**（用 `orientation` 会让手机横屏跳大屏样式）
- Manifest：`supports-screens` 全开、`touchscreen/microphone/leanback` 全 `required="false"`、
  同时有 `LAUNCHER` 与 `LEANBACK_LAUNCHER`

### 四条硬约束（改原生层时别踩）

| # | 约束 | 踩了的后果 |
|---|---|---|
| 1 | 虚拟域响应**必须带 `Access-Control-Allow-Origin: *`** | `fetch` 被 CORS 挡死（`<script src>` 不受影响，所以很容易漏测） |
| 2 | **绝不开 `setAllowUniversalAccessFromFileURLs(true)`** | 等于让网络下发的 JS 读整个私有目录 |
| 3 | 熔断状态**必须放 SharedPreferences**，不能放 localStorage | 热更 js 来自网络，逃生开关存在它能写的地方 = 没有逃生开关 |
| 4 | 内置相对路径**绝不能带 `?b=` query** | `android_asset` 会把 `js/games.js?b=0` 整个当文件名 |

另：音频**不走虚拟域**（`WebResourceResponse` 不支持 206 Range，mp3 无法 seek、duration 变 NaN），
落盘后直接用 `file:///data/data/<pkg>/files/hot/audio/x.mp3`。

### 逃生与调试

- `index.html?safe=1` 或 `#nohot` → 强制内置版
- `index.html#hotlog` → 看启动日志（设置面板连点 5 次「⚙️ 设置」标题也行）
- `index.html#hotbase=http://127.0.0.1:8899/hot/` → 指定热更源

### 两套通道并存期间

`hot/manifest.js`（老）+ `hot/pack/manifest.json`（新）CI 都要生成。
**老通道不能提前下线** —— 还有人在用 v1.1 的 APK。

---

## 3. 标准更新 SOP（照抄）

1. **改代码**：`index.html`、`css/`、`js/`、题目数据 —— **除了 `js/boot.js`**
2. **本地生成验证**（可选但推荐）：`node tools/gen-hot.mjs --out /tmp/hot-check`
3. **同步 assets**（改了 H5 就要做）：在**英语项目根目录**执行 `./sync-assets.sh --apply`
4. **提交**（⚠️ 见第 5 节的 `git add -A` 禁忌）
   ```bash
   git add -u
   git commit -m "..."
   ```
5. **推送**：`git push origin main`
6. **等 CI 约 3 分钟**，验证 `gh-pages` 有新 commit、`hot/manifest.js` 里的 `build` 值变了
7. **通知使用方「打开 App 两次」**：第一次后台下载，第二次打开就是新的

### 只改内容、不改界面时

题目数据（`js/data-*.js`）也在热更范围内，走同一套流程，无需区别对待。

---

## 3.5 看图识字（语文图库）专项

> 2026-09-18 出过一次大 bug，专开一节。

### 数据结构（都在 `chinese/js/`）

| 文件 | 内容 |
|---|---|
| `js/pics.js` | **两个结构必须一起保留**：`window.PICS`（字 → SVG，567 字）+ `window.PIC_CARDS`（327 项字表数组）。改图库时漏掉 `PIC_CARDS` 会连带坏别的功能 |
| `js/data-c1..c6.js` | 单元数据，IIFE 推入 `window.GRADES`，层级 `g → books[] → u[] → w[]`（年级 → 册 → 单元 → 字表）；字表项含 z / p / n / w / k 五个字段（字、拼音、笔画数、词语、图标） |
| `js/games.js` | 出题逻辑。`optsOf()`（约 L34）出选项；`picHTML()`（约 L71）渲染题干图 |

### 核心规则：为什么「同单元撞车」是致命的

看图识字的**题干是图**（`PICS[正确字]`），**选项是同单元的汉字文本**。
出题时干扰项**只从本单元取样**，所以：

- 同单元两个字共用一张 SVG → 孩子看到 A 的图，选 B 也"对" → **题无解**
- 跨单元同图 → 不影响答题，只是不够精致

实例（已修复）：一上·天地自然 `天=日`（共用太阳图）、`人=你=我=他`（共用一张人形图）。

### 双保险（都已上线）

1. **图库层**：`chinese/tools/check-pics.mjs` 校验同单元内 SVG 两两不同，撞车即 **exit 1**。改完图库必须跑：
   ```bash
   cd /workspace/chinese && node tools/check-pics.mjs      # 必须 0 撞车
   ```
2. **代码层**：`js/games.js` 的 `picDistractors()` —— 看图识字与挑战·看图选字都改用它出选项，
   强制排除「与正确答案共用同一张图」的字；同单元凑不够 4 个就向同年级其它单元借。
   即使将来图库再撞车，也不会出现死题（但会少一个"干扰项"，所以图库仍要修）。

### 画图约定（辨识锚点法）

- 尺寸 `viewBox="0 0 100 100"`，描边 `#37474f`、`stroke-width 3`，类名 `pic-svg`
- **每个字必须有一个独一无二的可视锚点**（独有构件 / 专属配色 / 构图差异），
  例：时=钟面、分=沙漏、秒=秒表红针、午=头顶太阳、月=月牙、日=太阳
- 保留原字原则：一组撞车里挑最贴切的原图保留，其余重画

### 真图体系（2026-09-19 起）

567 个字的目标状态是**每个字一张 AI 生成插画**（`img/uXXXX.webp`，384×384 WebP），
SVG 只当兜底。目前 165/567 已落地。

**图片来源三档**（`js/games.js` 的 `window.PIC_BASES`，按顺序试，全挂才回退 SVG）：

| 档 | 值 | 作用 |
|---|---|---|
| 1 | `https://local.hot/img/` | 资源包热更下来的图，最新，优先级最高 |
| 2 | `img/` | APK 内置 assets / 仓库自带 —— **首次安装没网也能看到图** |
| 3 | `https://…github.io/chinese/img/` | 远程兜底 |

顺序不能改：内置图排在资源包**之后**（否则热更新的新图会被内置旧图盖住），
排在 CDN **之前**（否则首次安装又没网时一项都拿不到）。

### 教材取图路线为什么放弃了（实测结论）

试过从人教社教材电子版抓页面图再裁出识字卡，工具留在
`tools/lib/pep.mjs` / `tools/fetch-book.mjs` / `tools/page-index.mjs` / `tools/coverage.mjs`，
**结论是不划算，别再往里投时间**：

| 指标 | 实测 | 判读 |
|---|---|---|
| R1 独占命中 | 1.8%（10/567） | 远低于 20% 门槛 |
| 字卡式缺口命中 | 0.3%（1/307） | 对最痛的缺口几乎无效 |
| G3–G6 缺口命中 | 11.5% | 高年级教材多为生字表/词语表，插画极少 |

两个硬伤：① 免费档视觉模型给的 bbox 又小又飘（整幅插画只框 0.4% 页面），裁不出干净的卡；
② 生字表/笔画表/偏旁表页会被当成"物体清单"读出来，裁出来是一张字的照片，比字卡还糟。

### 补图的重复动作（一条命令）

```bash
cd /workspace/chinese && ./tools/ship-pics.sh "批次说明"
```
它按顺序做：写回 `PIC_PHOTOS` → 重出 `pics.html` → `check-pics` 验收（不过就停）
→ 出资源包 → 同步壳 assets → 重新签名出包 → 提交推送。

生成器两个：`tools/gen-pics-zp.mjs`（智谱 CogView-4，¥0.01/张，并发 5，约 3.7s/张，
余额不足时 429）和 `tools/gen-pics.mjs`（Pollinations 免费，串行约 40s/张）。
两者共用 `tools/prompt.mjs` 的画风模板，**不要各改一份**。
排队优先级：字卡式（答案印在图上的那批）> 年级升序。

---

## 4. 什么能热更 / 什么必须出 APK

| 改动类型 | 热更 | 出 APK |
|---|---|---|
| 题目、生字、古诗、单词 | ✅ | |
| 界面、样式、玩法、bug 修复 | ✅ | |
| `js/boot.js`（**冻结文件**） | | ✅ |
| 原生层：语音识别、权限、图标、包名 | | ✅（可走 App 内一键升级） |

> `js/boot.js` 是热更引导器本身，层层校验，没法热更自己。改它 = 必须出新 APK。

**语文（v3.0）**：改完 `css/`、`js/`、`img/`、`audio/` 直接推即可，不用同步 assets。
加新玩法就丢一个 `js/game-xxx.js` 进去（`registerGame({id:…})` 会被自动扫到）。

### 出 APK 的流程（英语项目根目录）

```bash
./sync-assets.sh --apply   # 把 H5 同步进各 Android 工程
./build-apk.sh             # 构建并签名 5 个包（语文 1 个通用包 + 数学/英语各 Phone/TV）
```

产物校验：证书指纹必须始终是 `ffbc7d65ead51e3f…`（换了就意味着签名变了，用户必须卸载重装）。

---

## 5. 已知坑与禁忌（血泪清单）

| # | 坑 | 说明 |
|---|---|---|
| 1 | **`git add -A` 是禁忌** | 英语仓库同级有 `chinese/` `math/` 目录，会把别的科目塞进英语仓库。已在 `.gitignore` 加保护，但仍只用 `git add -u` |
| 2 | **`js/boot.js` 冻结** | 改它必须出新 APK |
| 3 | **哈希长度陷阱** | 生成器里文件的 `n` 必须等于 JS 的 `text.length`（UTF-16 码元数），**不能**是 UTF-8 字节数。写错会导致所有含中文的文件校验失败、热更全部回退 |
| 4 | **沙箱直连 GitHub 被墙** | `github.com` / `api.github.com` / `*.github.io` 全部 TLS 切断，必须走镜像。因此 `gh auth status` 报「token invalid」是**假象**，别信 |
| 5 | **Pages 必须网页端手动开一次** | CI 会自动创建 `gh-pages` 分支，但 Pages 服务不会自动启用 |
| 6 | **凭据要配在 `origin` 上** | 曾发生凭据被写进 `branch.main.remote`（且指向错误仓库）导致推不动、且 `git pull` 会把别的科目拉进来 |
| 7 | **语文大文件** | `strokes.js` / `pics.js` 接近或超过 localStorage 容量，设计成「尽力而为」，不要指望它们一定更新 |
| 8 | **`tmp/` 是本地验证残留** | 各科目下可能有 `tmp/`，已在 `.gitignore` 排除，不要提交 |
| 9 | **看图识字同单元图撞车** | 选项只从本单元取样，两字同图 = 死题。改图库必须跑 `node tools/check-pics.mjs`（见 §3.5） |
| 10 | **镜像 CDN 会骗人** | `ghproxy.net` 可能返回**过期的缓存**。验证 `gh-pages` 是否更新时，换 `ghfast.top` 再试一次，别只信一个镜像 |
| 11 | **旧壳工程已删** | 语文 `chinese-app` / `chinese-phone` 已于 2026-09-19 删除，只剩 `chinese-universal`。老脚本/老文档里出现这两个路径一律按过期处理 |
| 12 | **`update.json` 别删 `phone`/`tv` 字段** | 老版本 1.1 的 APK 里 `update.js` 是内置的、只读这两个字段；删了老用户就再也收不到升级提示。现在它们和 `apk` 指向同一个通用包 |

---

## 6. 故障排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 打开白屏 | 热更包有问题 | **不会持续白屏**：哨兵 1.5 秒内自动清空缓存重启用内置版；连错 2 次永久关闭热更 |
| 一直不更新 | Pages 连不上（国内访问 github.io 时快时慢） | 不影响使用，下次打开会重试 |
| 想强制用内置版 | — | 卸载重装，或用 adb 清 App 数据 |
| 语文笔顺/图库没更新 | 大文件存不下 | 属预期行为。真要强制更新笔顺数据，出一个新 APK |
| 看图识字选不出 / 两个选项都对 | 同单元两个字共用一张图（§3.5） | 进 `chinese/` 目录跑 `node tools/check-pics.mjs`，找出撞车组，按「辨识锚点法」重画后重新发布 |
| 推不动 | `origin` 上没凭据 | 检查 `remote.origin.url` 是否带凭据 |
| `pull` 拉进了别的科目 | `branch.main.remote` 被写错 | 已修复，勿再手动写该配置 |
| CI 出包但 `apk/` 目录空 | 4 个 Secrets 没填 | 填完重跑 CI 即可（不影响热更） |

### 查看热更日志（App 内）

设置面板里**连点 5 次「⚙️ 设置」标题** → 页面刷新并显示日志，可看到：
`base=`（热更地址）、`build=`（当前缓存版本）、`use cache` / `use builtin`、`BOOT OK`。

---

## 7. 敏感信息在哪（只记位置，不记值）

| 项目 | 位置 |
|---|---|
| keystore 本体 + 4 个 CI Secret 的值 | 项目内 `keystore/GitHub-Secrets填写清单.md`（该文件已被 git 忽略） |
| 4 个 Secret 名称 | `KEY_ALIAS`、`KEYSTORE_STORE_PASS`、`KEYSTORE_KEY_PASS`、`KEYSTORE_BASE64`（每个仓库都要填一遍） |
| GitHub token | 本地凭据文件 + 三个仓库的 `remote.origin.url` |

**安全建议**
- keystore 丢了 = 以后所有升级都要卸载重装，**务必备份整个 `keystore/` 到网盘**
- 日志里曾出现过 2 个不同的 GitHub token，来源不明的应尽快吊销
- 开启 GitHub 两步验证；全量热更等于远程代码执行，账号被盗影响所有已装设备

---

## 8. 状态快照（2026-09-19）

| 项目 | 状态 | CI 验证 |
|---|---|---|
| 英语 | 已迁至新仓库 `English`，v1.2（code 3） | ✅ `build=e0b3acd996009aca` |
| 语文 | **v2.0（code 3）· v3.0 通用壳**：手机/平板/TV 一个包，内容与玩法文件级热更 | ✅ 本地已出包验签；`hot/pack` build 由 CI 生成 |
| 数学 | v1.1 已推送（含 3 个积压 commit） | ✅ `build=98f7e09c27111810` |
| 三科 Pages | 均已开启（`gh-pages` 分支都在） | — |
| CI Secrets | 三个仓库各 4 个，状态待确认 | 未填不影响热更，只影响 `apk/` 产出 |

### 语文 v3.0 本期产出

| 项 | 值 |
|---|---|
| APK | `/workspace/apk/ChinesePlayground.apk` **1.9MB**（含内置 img/） |
| 包名 / 版本 | `com.example.chineseplayground` · versionCode 3 · versionName 2.0 |
| 签名 | v1 + v2，SHA-256 `ffbc7d65ead51e3faa2c456482d4077dd7bfb39d592fa59467d5ec336d923076`（与老包一致，可覆盖安装） |
| 内置内容 | 16 项 BUILTIN（index.html + css + js）**+ img/ 178 个 webp**（离线可见） |
| 资源包 | `assets.zip` 1.30MB（172 图）+ `code.zip` 540KB（17 个 js/css），build `c62cee00…` |
| 图库 | 165/567 字有真图；字卡式（答案印图上）由 332 → **261** |
| 验收 | 撞车 28 ≤ 基线；出题模拟 1728 题全绿；APK 内置内容离线跑 20 题 **0 破图 0 字卡** |

**仍在跑的**：免费通道继续补剩余约 380 张（约 40s/张，后台）。
每积累一批就 `./tools/ship-pics.sh` 发一次。
进度自查：`cd /workspace/chinese && node tools/check-pics.mjs`，看「字卡式图」是否趋近 0。

---

## 9. 以后改机制时

本手册与代码同源维护：**每次改动热更相关机制（含新增备用域名、调整缓存策略、改 CI 流程），
必须同步更新本手册并重新归档**，否则下一次执行会照着过期步骤走。
